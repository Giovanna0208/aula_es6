const authenticated = false;
const active = true;

console.log('tipo da variável authenticated:', typeof authenticated);
console.log('valor da variável authenticated:', authenticated);

console.log('tipo da variável active:', typeof active);
console.log('valor da variável active:', active);